import React from 'react';
import SearchBar from '../../components/SearchBar';

const Home = () => {
	return (
		<div className="box-border p-5 w-full h-full flex justify-center ">
			<SearchBar />
		</div>
	);
};

export default Home;
